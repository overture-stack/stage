Arranger integration work spanning 2026-07-31 to 2026-08-06, consolidated into one entry since none of it had been committed yet: migrated off the deprecated `hasValidConfig` check, fixed catalogue-scoped request routing, picked up several Arranger-side fixes, and settled the UI component library decision.

- Migrated the explorer page's startup config check off `hasValidConfig` to Arranger's `GET /introspection`:
  - `components/pages/explorer/checkArrangerCatalogue.ts` (new, with co-located BDD-style tests): pure helper checking a catalogue's presence and `status`, matching by catalogue ID rather than `documentType`, since a server can register several catalogues under the same document type
  - `components/pages/explorer/index.tsx`: swapped the `hasValidConfig` fetch for a `GET /introspection` call via `ajax`
  - `components/pages/explorer/getConfigError.tsx`: dropped the now-meaningless `index` field from the config-error display
  - `package.json`: added `@types/jest` (`^27.5.2`, matched to the installed `jest@27.5.1`), needed once a co-located `.test.ts` file put Jest globals inside `tsc`'s checked scope
- Fixed catalogue-scoped requests not reaching the right path: `components/utils/arrangerFetcher.ts` takes `catalogue` at creation time (from `explorer/index.tsx`'s `ARRANGER_CATALOGUE`) and builds the proxy path directly from it, rather than trusting or re-deriving anything from a per-call `url`. Traced via git history (commits `e689116`, `5826af9`) why the proxy exists at all (a Keycloak auth hop); full reasoning in `.dev/docs/arranger/README.md`
- `components/pages/explorer/RepoTable.tsx`: removed a dead `FieldName` import and a stale commented-out `defaultSorting` block referencing a different catalogue's field shape (`donors.submitter_donor_id`, `file.size`), leftover cruft unrelated to the donor catalogue this app now queries
- Picked up several Arranger-side fixes and documentation, recorded in `.dev/docs/arranger/README.md`: `docs/usage/` renamed to `docs/reference/`; server-side `GET /introspection/:catalogueId` and `/{catalogueId}/graphql` now also accept an unambiguous `documentType` as a fallback (`.dev/tech-debt.md`'s `ARRANGER_CATALOGUE` entry updated to prefer the real catalogue ID regardless); `Facets.tsx`'s `Aggregations` had its own catalogue-scoping bug, fixed upstream; `useArrangerConfig` shipped as `DataProvider`'s own config/validity resolution, superseding `checkArrangerCatalogue.ts` in principle but not adopted yet, since the hand-rolled version already works
- `.dev/roadmap.md`: recorded the decision to build Overture's generic UI component library on shadcn, over Mantine, Base UI/Radix used directly, React Aria Components, Ark UI/Park UI, and Untitled UI, with the reasoning inline; framework migration (off Next.js) remains open
- Rebase-pulled `origin/main` per instruction with a dirty working tree and no content overlap against the incoming commits: stashed (including untracked files), pulled, popped cleanly
