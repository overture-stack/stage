Bootstrapped the agentics devctx for this repo (previously had none): first working session here.

- `AGENTS.md`, `CLAUDE.md`, `DEVELOPMENT.md`: added, synced to agentics `0.10.0` (`6313e58`)
- `.dev/roadmap.md`: added, with an Active item to review the long-standing uncommitted/staged changes (Arranger version bump, npm script and `tsconfig.json` cleanup) as part of the next commit
- `.dev/tech-debt.md`: added, logging test co-location (`tests/` instead of source-adjacent) and the older Next.js 12/React 17 majors as known, non-blocking issues
- `.claude/settings.json` not copied: the credential-file hook already exists in global settings
- `.dev/roadmap.md`: added an Active item to move off Next.js and extract a generic Overture UI component library (ShadCN primary, Mantine secondary, both need research), and a Backlog item for canary-deploying Stage on Overture dependencies' `next` npm tags (Arranger now, Usher and the UI library later)
- Reviewed PR #257 (`overture-stack/stage`, docs/dev-services rewrite replacing the Prelude-based dev environment); drafted a general PR comment for the developer to post, covering points beyond what they'd already commented on directly
