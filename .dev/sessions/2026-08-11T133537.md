Removed the hardcoded `'donor'` catalogue assumption from the explorer page, per the data-model-agnostic requirement that applies to Stage like every other Overture service; this was intentionally temporary and already tracked in tech-debt.

- `components/pages/explorer/checkArrangerCatalogue.ts` renamed to `resolveArrangerCatalogue.ts` (test file renamed to match): `isArrangerCatalogueAvailable(introspection, knownId)` replaced with `resolveArrangerCatalogue(introspection)`, which picks the catalogue from `GET /introspection`'s `catalogs` map at runtime instead of checking a caller-supplied fixed ID. Resolves to the single available catalogue, `'none'` when there isn't one, `'ambiguous'` when more than one is available.
- `components/pages/explorer/index.tsx`: dropped the `ARRANGER_CATALOGUE` module constant and the module-level `arrangerFetcher` singleton it fed. The resolved catalogue ID now lives in component state, set from the introspection response; `arrangerFetcher` is created via `useMemo` once that resolves, keeping the same one-fetcher-per-catalogue lifetime the module-level version had rather than recreating it on every render. An `'ambiguous'` resolution logs a console warning and falls through to the existing generic configuration-error state; a real picker UI is out of scope for this pass.
- `.dev/tech-debt.md`: removed the now-resolved `ARRANGER_CATALOGUE` hardcode entry; added a new one for the deferred multicatalogue-picker case the `'ambiguous'` branch already detects but doesn't yet resolve for the user, since no live deployment needs it today.
- `.dev/docs/arranger/README.md`: updated the startup-config-check section to describe runtime resolution instead of the hardcoded constant, and fixed the stale `checkArrangerCatalogue.ts` filename reference in the `useArrangerConfig` section.

Verified: `tsc --noEmit` clean, `jest components/pages/explorer/` passes (5 new tests plus no regressions in the directory).

Adopted the atlas pattern for cross-UI review, per direct instruction: `.dev/docs/atlas/cross-ui-findings.md` (new), a running collection of what's portable vs. app-specific in other Overture/Overture-adjacent UI apps. Seeded with the first entry: read iMicroSeq portal-ui's `NavBar/Dropdown.tsx` directly while scoping a possible Arranger multicatalogue picker, found the component itself generic and portable (Stage already has equivalent `Button`/`chevron_down` primitives), but its actual usage hardcodes two datasets to separate routes by hand, exactly the pattern to avoid copying; a real picker needs its dropdown data generated from `resolveArrangerCatalogue`'s resolved catalogue IDs at runtime instead. `AGENTS.md` updated: a Project notes bullet noting Stage now reaches into sibling UI repos for this, and a dispatch-table line pointing at the new file.

Built the multicatalogue picker:

- `components/pages/explorer/CataloguePicker.tsx` (new): a dropdown autopopulated from whichever catalogue IDs `resolveArrangerCatalogue` reports as available, shown only when there's more than one. Mechanics adapted from the iMicroSeq finding above; Stage's own `Button`/`chevron_down` used directly, no new primitive needed.
- `components/pages/explorer/index.tsx`: the resolved/selected catalogue now lives in the `catalogue` URL query param via the existing `useUrlParamState` hook (already used elsewhere in this page for `filters`, matched rather than introducing new router-state handling), not local React state, so a choice is shareable and survives a refresh. A single available catalogue is still used automatically; more than one falls through to `CataloguePicker` until the URL names one of them.
- `components/pages/explorer/getConfigError.tsx`: simplified. Its old branching on `documentType` presence no longer applies now that `documentType` isn't passed to `ArrangerDataProvider` at all (see below); it now just reports "no active catalogue" when catalogue resolution genuinely fails, nothing else. Dropped the now-unused checklist rendering (`ListItem`/`WarningListItem`) that branch alone had used.
- `NEXT_PUBLIC_ARRANGER_DOCUMENT_TYPE` is deliberately unused for now: a same-session design question (should it override a single `documentType`, or restrict the picker to a subset of catalogue IDs, a different axis entirely) was raised and explicitly deferred so the picker itself wasn't blocked on resolving it; logged in `.dev/tech-debt.md`, `.dev/docs/arranger/README.md` updated to match current behavior (every catalogue resolves `documentType` automatically via `useArrangerConfig`).

Verified: `tsc --noEmit` clean, full `jest` suite passes (6 tests, no regressions).

Resolved the deferred env var question the same session: renamed `NEXT_PUBLIC_ARRANGER_DOCUMENT_TYPE` to `NEXT_PUBLIC_ARRANGER_CATALOGUES` (generic: Arranger can resolve either a catalogue ID or a document type from it), reused the existing comma-separated-list idiom already in this codebase (`NEXT_PUBLIC_ARRANGER_MANIFEST_COLUMNS`, `NEXT_PUBLIC_SSO_PROVIDERS`) rather than inventing a new one. `resolveArrangerCatalogue` takes an optional `allowedValues: string[]`, matching by catalogue ID or `documentType` (added to the introspection type), restricting which available catalogues are considered; empty/unset keeps full automatic behaviour. Updated across `global/config.ts`, `next.config.js`, `.env.schema`, `.env.test`, `.env.local`, `docs/setup.md`.

Caught a real bug before shipping it, by verifying against Arranger's actual source rather than trusting my own earlier doc summary: omitting `documentType` to let `ArrangerDataProvider`'s internal `useArrangerConfig` auto-resolve it doesn't work with `arrangerFetcher.ts` as `customFetcher`, two confirmed, compounding reasons (POST-only hardcoding ignoring `useArrangerConfig`'s `method: 'GET'`; a double-catalogue-URI-prefix mismatch between `useArrangerConfig`'s bootstrap-call convention and this fetcher's proxy-routing design). Coordinated directly with the Arranger session on both this and a separate question (confirmed `NEXT_PUBLIC_ARRANGER_INDEX` has no live or planned client-side consumer on their end either): they fixed a real gap in their own `APIFetcherFn` TSDoc as a result. Routed around it instead of fixing `arrangerFetcher.ts`: `explorer/index.tsx` now resolves `documentType` itself from the same `GET /introspection` response already fetched for catalogue discovery (a `documentType` per catalogue, straight from Arranger's response), passed to `ArrangerDataProvider` explicitly. Removed `NEXT_PUBLIC_ARRANGER_INDEX` entirely (unused since the `hasValidConfig` migration, now confirmed dead on Arranger's side too) from the same files.

Real cross-repo consequence, not fixed here: `overture/infra`'s Helm values across dev/sandbox/prod (six files) still set both old var names. Currently harmless, every real deployment has exactly one catalogue today so automatic resolution is unaffected either way, but logged as tech-debt since the values files now silently drift from what Stage's code actually reads; out of scope to touch unreviewed, especially with prod values involved.

Verified: `tsc --noEmit` clean, full `jest` suite passes (12 tests, no regressions).
