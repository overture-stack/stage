Ran agentics upstream-update check (`upgrading-adoption.md`), triggered by noticing the version tag was stale mid-conversation: `0.11.0` -> `0.13.0`. All gaps non-conflicting (stale, unmodified copies of the old template, nothing locally customized):

- `AGENTS.md` Interaction parameters: added the peer-proposal clause to "Check in before non-trivial decisions," and the new "External content that overlaps with a project you maintain" bullet.
- `AGENTS.md` dispatch table: added the `persistence-map.md` line; kept Stage's own Arranger-specific line, a legitimate addition on top, not a stale duplicate.
- `AGENTS.md` Memory and contribution hygiene: added the "default to project-scoped, not global" paragraph.
- Version tag updated to `0.13.0 | synced: 941106cb0952bc3ade6d86546c3430ae8bf248cf`.

New initialization question (added in 0.13.0, unanswered since Stage's project memory predates it): asked and recorded `roadmap_split: yes`. Applied as a real migration, not just a flag flip: moved the shadcn-decision reasoning (alternatives considered, why each was ruled out) out of `.dev/roadmap.md` into `.dev/docs/atlas/roadmap/ui-component-library.md`, Stage's first atlas file, with `.dev/docs/atlas/index.md` created alongside it. The roadmap entry itself now states status in a couple of sentences and cross-links. Other roadmap entries left inline: neither carries reasoning/alternatives dense enough to warrant relocating, just necessary status detail.

Cleaned up a stale project-memory note found in the process: `project_stage_overview.md` still said "no agentics devctx adopted yet" directly above a section confirming it was adopted back on 2026-07-30, and separately recorded the synced version as `0.10.0` when `AGENTS.md`'s own tag already said `0.11.0`, a second version drifting out of sync with the first. Removed the outdated section and stopped restating the version number in memory at all: `AGENTS.md`'s tag is the one live source of truth for that.

Nothing conflicting found; no `.dev/agentics-overrides.md` needed.

Re-ran the check on explicit request ("update your agentics") rather than trusting this was already settled from minutes earlier: caught two real misses the first pass had missed by only diffing the *changed* sections instead of every inlined section byte-for-byte against the current template. `## Memory and contribution hygiene` was missing an illustrative sentence ("Confirmed directly: an agent recorded...") present in the canonical text; `## Initialization` question 6 still carried a stale em dash the current template had already cleaned up to a colon, invisible unless diffed directly since it predated today's edits entirely. Both fixed. Agentics itself has a same-day, still-uncommitted draft change addressing exactly this failure mode (an empty tag-vs-HEAD diff isn't proof a session has actually seen current content) and making "update your agentics" always run for real rather than skip on an assumed-already-checked basis; not yet a numbered release, so no new sync target, but the behavior it asks for is what this second pass just did.
